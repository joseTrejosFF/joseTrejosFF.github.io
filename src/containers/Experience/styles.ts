import styled from "styled-components";
import { FlexColContainer } from "components/GlobalStyles";

export const ExperienceContainer = styled(FlexColContainer)`
  padding: 4rem 0 5rem 0;

  @media (max-width: 768px) {
    padding-bottom: 1rem;
  }
`;
